#include <string.h>

int *getKvalue(char *string)
{
    int n = strlen(string);
    int size = 0;
    int k_val[10] = {0};
    k_val[0] = 0;
    for (int i = 1; i < n; i++)
    {
    }
}

int main()
{
    return 0;
}